<?xml version="1.0" encoding="utf-8"?><!--This file just contains each of the activities that will be running in the-->
<!--app at some point. We will at more as needed-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kinashe.kinasheandroid">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlacesActivity"></activity>
        <activity android:name=".TransportationActivity"></activity>
        <activity android:name=".AddBusinessActivity"></activity>
        <activity android:name=".SingleBusinessActivity"></activity>
    </application>

</manifest>